        'Creates dynamic list of moves that all share the score of the best move.
        Dim TiedBestScores As New List(Of Move)
        TiedBestScores.Capacity = 25


                            TiedBestScores.Clear() 'Resets list of tied moves.
                        ElseIf CurrentMove.Score = BestMove.Score Then
                            'Score has been tied - add it to the list of tied moves.
                            CurrentMove.OldMoveX = PieceMoves(n, 0)
                            CurrentMove.OldMoveY = PieceMoves(n, 1)
                            CurrentMove.NewMoveX = PieceMoves(n, 2)(0)
                            CurrentMove.NewMoveY = PieceMoves(n, 2)(1)
                            TiedBestScores.Add(CurrentMove)



If TiedBestScores.Count > 0 Then 'Tie of best move detected.
                    Console.WriteLine("Tie of " & TiedBestScores.Count + 1 & " Moves. Picking Random Move.")
                    Dim rand As New Random()
                    Dim RNDint As Byte = rand.Next(TiedBestScores.Count + 1)
                    'Makes the best move a random move in the TiedBestScores list.
                    If RNDint <> TiedBestScores.Count Then
                        BestMove.Score = TiedBestScores(RNDint).Score
                        BestMove.OldMoveX = TiedBestScores(RNDint).OldMoveX
                        BestMove.OldMoveY = TiedBestScores(RNDint).OldMoveY
                        BestMove.NewMoveX = TiedBestScores(RNDint).NewMoveX
                        BestMove.NewMoveY = TiedBestScores(RNDint).NewMoveY
                    End If
                End If